import { Mahadi } from "@/context";
import React, { useContext } from "react";

const Navigation = () => {
  const {} = useContext(Mahadi);

  return <nav>this is navigation</nav>;
};

export default Navigation;
